rootProject.name = "TaoKDao-API"

include ':api_public',
        ':api_skin',
        ':shadow-constant'

rootProject.children.each {
    /**设置构建脚本文件名*/
    it.buildFileName = "tkd-"+it.name + '.gradle'
}